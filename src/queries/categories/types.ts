export type ICreateCategories = {
  name: string;
  description: string;
  thumbnail_url: string;
  cover_url: string;
};

export type IUpdateCategories = {
  name: string;
  description: string;
  thumbnail_url: string;
  cover_url: string;
};
