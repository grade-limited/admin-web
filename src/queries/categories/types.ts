export type ICreateBrand = {
  name: string;
  description: string;
  thumbnail_url: string;
  cover_url: string;
};

export type IUpdateBrand = {
  name: string;
  description: string;
  thumbnail_url: string;
  cover_url: string;
};
